[pytest]
testpaths = tests
asyncio_mode = auto
# Surface warnings; a DeprecationWarning from pymodbus is how we would first
# learn that a pinned dependency has moved under us.
filterwarnings =
    default
